# Agent Health Monitor - x402 API Configuration
#
# Copy to .env and fill in your values:
#   cp .env.example .env

# Required: Your wallet address to receive USDC payments
PAYMENT_ADDRESS=0xYOUR_WALLET_ADDRESS_HERE

# Optional: Blockscout/Basescan API key (free at basescan.org/apis)
# Increases rate limits from ~1 req/5s to ~5 req/s
BASESCAN_API_KEY=

# Optional: x402 facilitator URL
# Default: https://x402.org/facilitator
FACILITATOR_URL=https://x402.org/facilitator

# Optional: Price per health check (default: $0.50)
PRICE_USD=$0.50

# Optional: CAIP-2 network identifier (default: Base Sepolia for x402.org facilitator)
# Base Sepolia (testnet): eip155:84532  (works with x402.org/facilitator)
# Base mainnet:           eip155:8453   (requires CDP facilitator — see FACILITATOR_URL)
NETWORK=eip155:84532

# Optional: Server port (default: 4021)
PORT=4021
